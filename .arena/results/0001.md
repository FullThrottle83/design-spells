# Arena result 0001: run existing test suite with reproducible Chromium

**Status:** DONE
**Task brief:** `.arena/tasks/0001.md`
**Branch / base:** `arena/01a0de89-design-spells` from `main` @ `8bc1de55d6f649f4a86e66600dd2dc3c24f8f6ee` (brief's baseline `fd4a7fa` stale; actual HEAD verified). PR #47 untouched; no prior PR on this branch.
**Commit / PR:** see PR description (SHA recorded there) — https://github.com/FullThrottle83/design-spells/pull/<PR_NUMBER>

## Changed
- `scripts/arena-browser/` — committed bootstrap (`playwright-core@1.63.0` + `@sparticuz/chromium@153.0.0` into session-local `/tmp` scratch), shared launch options, security self-check, README
- `playwright.arena.config.mjs` — derives from repo config; adds executablePath, `chromiumSandbox: true`, loopback-only launch flags, `serviceWorkers: "block"`; discovery/assertions untouched
- `docs/evidence/arena-task-0001/` — REPORT.md, `verify-chromium.out`, raw runner log
- No tracked file modified (git diff empty); no spell/app/assertion changes

## Checks
- `npm ci` → PASS (exit 0, 3 packages, 0 vulnerabilities)
- `npm run build` → PASS (exit 0; spells.json 463930 bytes; worktree clean)
- `npm run test:build` (Python, reported separately) → PASS: **76 passed / 0 failed / 0 skipped** (`Ran 76 tests in 2.114s`)
- `node scripts/arena-browser/bootstrap.mjs` → PASS (Chromium 153.0.8010.0; AL2023 libs extracted via package's own `inflate()` — package only auto-extracts under Lambda)
- `node scripts/arena-browser/verify-chromium.mjs` → PASS: 11/11 (no forbidden flags — hard-fail guard; `chromiumSandbox: true` works, no `--no-sandbox`; loopback nav/subresource allowed; external hostname, external IP, external subresource all blocked; SW registration blocked)
- `npx playwright test --config playwright.arena.config.mjs` → PASS: **423 passed / 0 failed / 0 skipped** in 5.9m; `--list` = 423 tests in 17 files, identical discovery to repo config; `cross-browser.spec.mjs` excluded by the repo's own `testIgnore` (dedicated Firefox/WebKit CI job), reported as intended-excluded
- `git diff --check` → PASS (exit 0)

## Not done / uncertainty
- `npm test`'s browser layer ran via the arena config (task's fallback) because the repo config cannot enforce the network boundary/SW block; rationale in evidence REPORT.md
- `chrome://sandbox` unavailable in this build; sandbox evidence = effective argv lacking `--no-sandbox` (read from `/proc`)
- Scratch bootstrap is session-local by design; re-run bootstrap in a fresh session

## Required human action
- NONE (do not merge per task)

**Scope and secret review:** PASS — only the four new paths above; no credentials in scripts or browser env; `git status` limited to intended files.
**Evidence:** `docs/evidence/arena-task-0001/REPORT.md`, `verify-chromium.out`, `raw/playwright-arena-run.log` (423 `✓`, zero `✘`/`flaky`/`skipped` lines).
